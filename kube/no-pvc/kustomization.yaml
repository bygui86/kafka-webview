namespace: kafka

resources:
  - podSecurityPolicy.yaml
  - rbac.yaml
  - configMap.yaml
  - deployment.yaml
  - service.yaml
  - prometheusServiceMonitor.yaml

commonLabels:
  app: kafka-webview
  group: kafka

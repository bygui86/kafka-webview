#namespace: change-me

resources:
  - podSecurityPolicy.yaml
  - rbac.yaml
  - configMap.yaml
  - secret.yaml
  - persistentVolumeClaim.yaml
  - deployment.yaml
  - service.yaml
  - prometheusServiceMonitor.yaml

commonLabels:
  app: kafka-webview
  group: kafka
